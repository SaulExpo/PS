'use strict';

/**
 * profesional-user-app service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::profesional-user-app.profesional-user-app');
